import express from "express"

// import { addProduct, updateProductById, deleteProduct, getAllProducts, getProductById  } from "../services/store-services"

export default (router: express.Router) => {
//     router.post('/store', addProduct);

//     router.patch('/store/:id', updateProductById);

//     router.get('/store', getAllProducts);

//     router.get('/store/:id', getProductById);

//     router.delete('store/:id', deleteProduct);
//
 }